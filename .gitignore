node_modules/
bot/node_modules/
__pycache__/
*.pyc
.env
/tmp/
*.log
*.pid
*.tmp
.DS_Store

# Minecraft / Paper runtime artifacts
server/logs/
server/crash-reports/
server/cache/
server/plugins/
!server/start.sh
server/config/
server/libraries/
server/versions/
server/hermescraft-world/
server/hermescraft-world_nether/
server/hermescraft-world_the_end/
server/*.jar
server/*.json
server/*.yml
server/server.properties
server/eula.txt
server/session.lock
server/usercache.json
server/*.dat_old
server/*/session.lock

# Local agent runtime state
data/locations-*.json
!data/locations.json

# NLP.js trained model (generated locally by bot)
bot/model.nlp

# Schematic build progress checkpoints (generated locally by bot)
bot/build_states/

# Cursor / editor local state
.cursor/

# Editor / screenshots
*.swp
*.swo
/tmp/mc_screenshot_*.png

# /post-merge unification (agorokh/agent-factory#246 follow-up):
# allow the post-merge skill surfaces under .claude/.cursor/.codex even
# when those parent dirs are otherwise ignored, so /post-merge works
# identically across all agent surfaces in this repo.
!.claude/skills/post-merge/
!.claude/skills/post-merge/**
!.cursor/skills/post-merge/
!.cursor/skills/post-merge/**
!.codex/skills/post-merge/
!.codex/skills/post-merge/**
